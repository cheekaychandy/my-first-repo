<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.bdd</groupId>
    <artifactId>cuc_sel_junit_allure</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <!-- Java Compiler Version -->
        <maven.compiler.source>17</maven.compiler.source> <!-- Specifies the Java version for source code compatibility -->
        <maven.compiler.target>17</maven.compiler.target> <!-- Specifies the Java version for generated bytecode compatibility -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding> <!-- Sets the encoding for source files -->

        <!-- Dependency Versions -->
        <allure.version>2.28.0</allure.version>  <!-- Version of Allure for reporting -->
        <selenium.version>4.9.0</selenium.version> <!-- Version of Selenium for browser automation -->
        <jmh.version>1.37</jmh.version> <!-- Version of JMH for benchmarking -->
        <cucumber.version>7.12.0</cucumber.version> <!-- Version of Cucumber for BDD testing -->
        <junit.version>5.9.2</junit.version> <!-- Version of JUnit for unit testing -->
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Allure BOM -->
            <dependency>
                <groupId>io.qameta.allure</groupId>
                <artifactId>allure-bom</artifactId>
                <version>${allure.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Selenium -->
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>${selenium.version}</version>
        </dependency>

        <!-- Java 17 Compatibility -->
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>${jmh.version}</version>
        </dependency>

        <!-- Cucumber -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>${cucumber.version}</version>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit</artifactId>
            <version>${cucumber.version}</version>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>messages</artifactId>
            <version>21.0.1</version> <!-- Use a version compatible with all dependencies -->
        </dependency>

        <!-- JUnit -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
        </dependency>

        <!-- Allure -->
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-java-commons</artifactId>
            <version>${allure.version}</version>
        </dependency>
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-cucumber7-jvm</artifactId>
            <version>${allure.version}</version>
        </dependency>
        <!-- WebDriverManager -->
        <dependency>
            <groupId>io.github.bonigarcia</groupId>
            <artifactId>webdrivermanager</artifactId>
            <version>5.4.1</version>
        </dependency>
    </dependencies>
</project>




------------------------

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.bdd</groupId>
    <artifactId>cuc_sel_junit_allure</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <!-- Java Compiler Version -->
        <maven.compiler.source>17</maven.compiler.source> <!-- Specifies the Java version for source code compatibility -->
        <maven.compiler.target>17</maven.compiler.target> <!-- Specifies the Java version for generated bytecode compatibility -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding> <!-- Sets the encoding for source files -->

        <!-- Dependency Versions -->
        <allure.version>2.28.0</allure.version>  <!-- Version of Allure for reporting -->
        <selenium.version>4.9.0</selenium.version> <!-- Version of Selenium for browser automation -->
        <jmh.version>1.37</jmh.version> <!-- Version of JMH for benchmarking -->
        <cucumber.version>7.12.0</cucumber.version> <!-- Version of Cucumber for BDD testing -->
        <junit.version>5.9.2</junit.version> <!-- Version of JUnit for unit testing -->
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Allure BOM -->
            <dependency>
                <groupId>io.qameta.allure</groupId>
                <artifactId>allure-bom</artifactId>
                <version>${allure.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Selenium -->
        <dependency>
            <groupId>org.seleniumhq.selenium</groupId>
            <artifactId>selenium-java</artifactId>
            <version>${selenium.version}</version>
        </dependency>

        <!-- Java 17 Compatibility -->
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>${jmh.version}</version>
        </dependency>

        <!-- Cucumber -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>${cucumber.version}</version>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit</artifactId>
            <version>${cucumber.version}</version>
        </dependency>
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>messages</artifactId>
            <version>21.0.1</version> <!-- Use a version compatible with all dependencies -->
        </dependency>

        <!-- JUnit -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
        </dependency>

        <!-- Allure -->
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-java-commons</artifactId>
            <version>${allure.version}</version>
        </dependency>
        <dependency>
            <groupId>io.qameta.allure</groupId>
            <artifactId>allure-cucumber7-jvm</artifactId>
            <version>${allure.version}</version>
        </dependency>
        <!-- WebDriverManager -->
        <dependency>
            <groupId>io.github.bonigarcia</groupId>
            <artifactId>webdrivermanager</artifactId>
            <version>5.4.1</version>
        </dependency>
    </dependencies>
</project>


-------------------------------------